services:
  # Backend - Web Service
  - type: web
    name: opticapp-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --include=dev && npm run build && npm run db:init
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        value: postgresql://opticapp_database_user:fSnX6LmogG58c0ecbqaIYumc17FkTFZx@dpg-d4dpejfdiees73bp2sl0-a/opticapp_database

